{
  "name": "vite-react-stackblitz",
  "version": "0.0.1",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "react": "^18.0.0",
    "react-dom": "^18.0.0",
    "@workday/canvas-kit-css": "^X.Y.Z",      // add this if you want the Workday CSS
    "@workday/canvas-kit-react": "^X.Y.Z",    // add if you actually use the Canvas React components
    "json5": "^2.2.1"                         // only if you actively need JSON5 in your code
    // ... other libraries you might use
  },
  "devDependencies": {
    "vite": "^4.0.0"
  }
}
